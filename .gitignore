# Rust build artifacts
/target/

# Locally assembled .app bundles (from scripts/package-macos.sh)
*.app/

# Debug logs and transient screenshots
*.log
screenshot*.png
shot-*.png
crop*.png
*shot-panel.png
*panel.png
shot-*.png

# ZCode session artifacts
.zcode/

# minisign keypair (generated locally by `cargo run --example
# gen_minisign_keypair`). NEVER commit - minisign.key is the private signing
# key; minisign.pub is only needed to bake the pubkey into the app.
minisign.key
minisign.pub
__pycache__/
